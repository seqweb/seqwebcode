# SeqWeb Project Dependencies
# Minimal requirements for the polyglot pipeline tools

# Core dependencies
# Currently using only standard library modules:
# - argparse, subprocess, pathlib, typing (all built-in)
# - No external dependencies required yet

# Future dependencies (uncomment as needed):
# requests>=2.31.0          # For HTTP operations
# rdflib>=6.3.0             # For RDF processing
# sparqlwrapper>=2.0.0      # For SPARQL queries

# Development dependencies (optional)
# pytest>=7.4.0             # For testing
# black>=23.0.0             # For code formatting
# mypy>=1.5.0               # For type checking
# flake8>=6.0.0             # For linting 